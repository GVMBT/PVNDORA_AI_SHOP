# Команды бота Telegram

## Основные команды

### `/start`
Запускает бота и показывает приветственное сообщение.

### `/help`
Показывает справку по использованию бота с кнопками быстрого доступа:
- ❓ **FAQ** - Часто задаваемые вопросы
- 📄 **Условия** - Пользовательское соглашение
- 🆘 **Поддержка** - Связаться с поддержкой

### `/my_orders`
Показывает историю покупок пользователя.

### `/wishlist`
Показывает сохраненные товары (избранное).

### `/referral`
Показывает реферальную ссылку пользователя.

## Команды для FAQ, Terms и Support

### `/faq`
Открывает страницу FAQ в веб-приложении с кнопкой для быстрого доступа.

### `/terms`
Открывает страницу пользовательского соглашения в веб-приложении с кнопкой для быстрого доступа.

### `/support`
Открывает страницу поддержки/контактов в веб-приложении с кнопкой для быстрого доступа.

## Где найти FAQ, Terms и Support

### В боте (Telegram):
1. **Команда `/help`** - показывает кнопки для быстрого доступа к FAQ, Terms и Support
2. **Команды `/faq`, `/terms`, `/support`** - открывают соответствующие страницы напрямую
3. **Меню бота** - все команды доступны через меню команд Telegram (кнопка меню в чате)

### В веб-приложении (Mini App):
- **FAQ**: Открывается через `?startapp=faq`
- **Terms**: Открывается через `?startapp=terms`
- **Support/Contacts**: Открывается через `?startapp=contacts`

## Технические детали

### Реализация команд
- Файл: `src/bot/handlers/commands.py`
- Все команды используют `WebAppInfo` для открытия страниц в Mini App
- Кнопки локализованы через `get_text()` с поддержкой всех языков

### Локализация
- Кнопки используют локализованные тексты из `locales/*.json`
- Ключи локализации:
  - `faq.title` - заголовок FAQ
  - `terms.title` - заголовок Terms
  - `contacts.support` - заголовок Support
  - `btn_open_faq`, `btn_open_terms`, `btn_open_support` - тексты кнопок

### Регистрация команд
- Команды зарегистрированы в `bot_setup/bot_info.json`
- Описания команд локализованы для всех поддерживаемых языков
