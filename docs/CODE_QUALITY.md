# Инструменты проверки качества кода

## 📋 Установка

### Python инструменты

**Для локальной разработки:**
```bash
pip install -r requirements-dev.txt
```

**Для production (Vercel):**
```bash
pip install -r requirements.txt  # Без dev-инструментов
```

> ⚠️ **Важно:** `requirements.txt` используется Vercel при деплое. Dev-инструменты (black, isort, mypy, pylint, bandit) находятся в `requirements-dev.txt` и **не попадают на production**, чтобы не увеличивать размер бандла (лимит 250 MB).

### TypeScript/JavaScript инструменты
```bash
npm install
```

## 🚀 Использование

### Автоматическое форматирование (VSCode)

Все инструменты настроены для **автоматического форматирования при сохранении** файла.

Просто сохраните файл (`Ctrl+S` / `Cmd+S`) и код будет автоматически отформатирован.

### Ручной запуск проверок

#### Python

```bash
# Форматирование кода
black .

# Сортировка импортов
isort .

# Проверка форматирования (без изменений)
black --check .
isort --check-only .

# Линтинг
ruff check .
pylint api core scripts

# Проверка типов
mypy api core

# Проверка безопасности
bandit -r api core

# Все проверки сразу
python scripts/check_code_quality.py

# Все проверки с автоисправлением
python scripts/check_code_quality.py --fix
```

#### TypeScript/JavaScript

```bash
# Форматирование кода
npm run format

# Проверка форматирования
npm run format:check

# Линтинг
npm run lint

# Линтинг с автоисправлением
npm run lint:fix

# Проверка типов (обычная)
npm run typecheck

# Проверка типов (строгая)
npm run typecheck:strict

# Все проверки сразу
npm run check:all
```

## 📁 Конфигурационные файлы

- **`.vscode/settings.json`** - настройки VSCode для автоформатирования
- **`pyproject.toml`** - конфигурация black, isort, mypy, pylint, bandit
- **`.prettierrc`** - конфигурация Prettier для TypeScript/JavaScript
- **`.mypy.ini`** - конфигурация mypy (альтернатива pyproject.toml)
- **`tsconfig.strict.json`** - строгая проверка TypeScript

## 🔧 Что проверяет каждый инструмент

### Python

| Инструмент | Что проверяет |
|-----------|---------------|
| **black** | Форматирование кода (отступы, пробелы, длина строк) |
| **isort** | Сортировка и группировка импортов |
| **ruff** | Быстрый линтер (ошибки, стиль, неиспользуемые переменные) |
| **pylint** | Расширенная проверка (сложность, best practices) |
| **mypy** | Проверка типов (type hints) |
| **bandit** | Уязвимости безопасности |

### TypeScript/JavaScript

| Инструмент | Что проверяет |
|-----------|---------------|
| **Prettier** | Форматирование кода |
| **ESLint** | Линтинг (ошибки, стиль, React hooks) |
| **TypeScript** | Проверка типов |

## ⚙️ Настройки VSCode

### Автоформатирование при сохранении

Включено автоматически через `.vscode/settings.json`:
- Python → форматируется через `black`
- TypeScript/JavaScript → форматируется через `Prettier`
- Импорты → автоматически сортируются

### Ручное форматирование

- **Python**: `Shift+Alt+F` (или `Cmd+Shift+P` → "Format Document")
- **TypeScript**: `Shift+Alt+F` (или `Cmd+Shift+P` → "Format Document")

## 🎯 Рекомендации

1. **Установите dev-зависимости для локальной разработки**:
   ```bash
   pip install -r requirements-dev.txt
   ```

2. **Всегда форматируйте перед коммитом**:
   ```bash
   python scripts/check_code_quality.py --fix
   npm run format && npm run lint:fix
   ```

2. **Используйте строгую проверку TypeScript** для новых файлов:
   ```bash
   npm run typecheck:strict
   ```

3. **Проверяйте безопасность** перед деплоем:
   ```bash
   bandit -r api core
   ```

## 🐛 Решение проблем

### Инструмент не найден

Убедитесь, что установлены все зависимости:
```bash
pip install -r requirements.txt
npm install
```

### Конфликты форматирования

Если black и isort конфликтуют, используйте профиль isort для black:
```bash
isort --profile black .
```

### Много ошибок mypy

Mypy может быть строгим. Для начала отключите строгие проверки в `pyproject.toml`:
```toml
[tool.mypy]
disallow_untyped_defs = false
```

### Prettier не работает

Убедитесь, что установлен Prettier:
```bash
npm install --save-dev prettier
```

## 📚 Дополнительные ресурсы

- [Black Documentation](https://black.readthedocs.io/)
- [Ruff Documentation](https://docs.astral.sh/ruff/)
- [Prettier Documentation](https://prettier.io/docs/en/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
