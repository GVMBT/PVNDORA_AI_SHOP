# Roadmap: Альтернативные Методы Авторизации

## Текущий Статус

**Реализовано:**
- ✅ Telegram Mini App авторизация (initData)
- ✅ Telegram Login Widget (браузерная версия)

**В разработке:**
- 🔄 Email/Password регистрация
- 🔄 Guest Mode (анонимные покупки)
- 🔄 Magic Links (без паролей)

## Планируемые Улучшения

### 1. Email/Password Авторизация

**Приоритет:** Высокий  
**Оценка:** 2-3 недели  
**Статус:** Планируется

#### Функциональность

- Регистрация с email и паролем
- Восстановление пароля через email
- Связывание email с существующим Telegram-аккаунтом
- Опциональная двухфакторная аутентификация (2FA)

#### Технические детали

**Backend:**
- Новый endpoint: `POST /api/webapp/auth/register`
- Новый endpoint: `POST /api/webapp/auth/login`
- Новый endpoint: `POST /api/webapp/auth/forgot-password`
- Новый endpoint: `POST /api/webapp/auth/reset-password`
- Хеширование паролей: bcrypt (12 rounds)
- Email верификация: токены с TTL 24 часа

**Database:**
```sql
ALTER TABLE users ADD COLUMN email VARCHAR(255) UNIQUE;
ALTER TABLE users ADD COLUMN password_hash VARCHAR(255);
ALTER TABLE users ADD COLUMN email_verified BOOLEAN DEFAULT FALSE;
ALTER TABLE users ADD COLUMN email_verification_token VARCHAR(255);
ALTER TABLE users ADD COLUMN password_reset_token VARCHAR(255);
ALTER TABLE users ADD COLUMN password_reset_expires_at TIMESTAMPTZ;
```

**Frontend:**
- Компонент `EmailRegisterForm.tsx`
- Компонент `EmailLoginForm.tsx`
- Компонент `ForgotPasswordForm.tsx`
- Интеграция в `LoginPage.tsx`

#### Безопасность

- Rate limiting: 5 попыток входа в минуту
- Password requirements: минимум 8 символов, буквы + цифры
- Email verification обязательна для критических операций
- Password reset: одноразовый токен, TTL 1 час

### 2. Guest Mode (Гостевой Режим)

**Приоритет:** Средний  
**Оценка:** 1-2 недели  
**Статус:** Планируется

#### Функциональность

- Покупки без регистрации
- Временный session token для гостя
- Возможность "поднять" гостевой аккаунт (связать с email/Telegram позже)
- Сохранение истории покупок после регистрации

#### Технические детали

**Backend:**
- Новый endpoint: `POST /api/webapp/auth/guest-session`
- Генерация временного guest_id (UUID)
- Session token для гостя (TTL 7 дней)
- Endpoint для "поднятия" аккаунта: `POST /api/webapp/auth/upgrade-guest`

**Database:**
```sql
CREATE TABLE guest_sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  guest_id VARCHAR(255) UNIQUE NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  expires_at TIMESTAMPTZ NOT NULL,
  upgraded_to_user_id UUID REFERENCES users(id)
);

-- Связь заказов с гостями
ALTER TABLE orders ADD COLUMN guest_id VARCHAR(255);
CREATE INDEX idx_orders_guest_id ON orders(guest_id);
```

**Frontend:**
- Автоматическое создание guest session при первом визите
- Модалка "Хотите зарегистрироваться?" после первой покупки
- Компонент `UpgradeGuestAccount.tsx`

#### Ограничения Guest Mode

- ❌ Нет доступа к истории заказов (пока не зарегистрируются)
- ❌ Нет реферальной программы
- ❌ Нет сохранения настроек валюты/языка
- ✅ Можно покупать товары
- ✅ Можно получать товары (доставка работает)

### 3. Magic Links (Вход по Ссылке)

**Приоритет:** Низкий  
**Оценка:** 1 неделя  
**Статус:** Возможно в будущем

#### Функциональность

- Вход без пароля через ссылку в email
- Одноразовая ссылка с TTL 15 минут
- Автоматический вход после клика по ссылке

#### Технические детали

**Backend:**
- Новый endpoint: `POST /api/webapp/auth/magic-link`
- Генерация одноразового токена
- Отправка email через SMTP
- Новый endpoint: `GET /api/webapp/auth/verify-magic-link?token=...`

**Database:**
```sql
CREATE TABLE magic_link_tokens (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) NOT NULL,
  token VARCHAR(255) UNIQUE NOT NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  used BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**Frontend:**
- Компонент `MagicLinkForm.tsx`
- Страница подтверждения: `/auth/magic-link-verify`

### 4. OAuth Провайдеры (Google, GitHub)

**Приоритет:** Низкий  
**Оценка:** 2 недели  
**Статус:** Возможно в будущем

#### Функциональность

- Вход через Google аккаунт
- Вход через GitHub аккаунт
- Автоматическая связка с существующим аккаунтом (по email)

#### Технические детали

**Backend:**
- Интеграция с OAuth 2.0 провайдерами
- Новый endpoint: `GET /api/webapp/auth/oauth/{provider}`
- Callback endpoint: `GET /api/webapp/auth/oauth/{provider}/callback`

**Libraries:**
- `authlib` для Python (OAuth 2.0 client)

**Frontend:**
- Кнопки "Войти через Google/GitHub"
- Интеграция в `LoginPage.tsx`

## Приоритизация

### Phase 1: Основные Методы (Q1 2025)

1. ✅ Email/Password авторизация
2. ✅ Guest Mode
3. ✅ Интеграция в существующий UI

### Phase 2: Улучшения (Q2 2025)

1. Magic Links
2. Улучшение безопасности (2FA, rate limiting)
3. Улучшение UX (автоматическая связка аккаунтов)

### Phase 3: Дополнительно (Q3 2025)

1. OAuth провайдеры (Google, GitHub)
2. Биометрическая аутентификация (WebAuthn)
3. Single Sign-On (SSO) для корпоративных клиентов

## Миграция Существующих Пользователей

### Стратегия

1. **Обратная совместимость:** Существующие Telegram-пользователи продолжают работать
2. **Дополнительная регистрация:** Пользователи могут добавить email к существующему аккаунту
3. **Автоматическая связка:** Если email уже используется, связываем аккаунты

### UI/UX

- Опциональная форма "Добавить email" в профиле
- Уведомление: "Хотите добавить email для восстановления доступа?"
- Плавный переход без принуждения

## Безопасность

### Общие Принципы

1. **Rate Limiting:** На все endpoints авторизации
2. **Password Hashing:** bcrypt с 12 rounds минимум
3. **Token Expiration:** Все токены имеют TTL
4. **HTTPS Only:** Все запросы через HTTPS
5. **Email Verification:** Для критических операций

### Rate Limits

- Login: 5 попыток в минуту
- Registration: 3 попытки в час
- Password Reset: 3 попытки в час
- Magic Link: 5 попыток в час

## Тестирование

### Unit Tests

- Password hashing
- Token generation/verification
- Email validation

### Integration Tests

- Полный flow регистрации
- Полный flow входа
- Password reset flow
- Guest mode → Upgrade flow

### Security Tests

- SQL injection
- XSS
- CSRF
- Brute force protection

---

**Последнее обновление:** 2024.12.13  
**Статус:** Планирование
