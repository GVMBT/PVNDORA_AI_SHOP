# Как запустить проверку SonarQube во всем проекте

## Способ 1: SonarLint в VS Code (рекомендуется для разработки)

**Уже работает!** SonarLint автоматически анализирует файлы в реальном времени.

### Что показывается:
- ✅ Проблемы в Problems панели VS Code
- ✅ Подсветка проблем прямо в коде
- ✅ Рекомендации по исправлению

### Преимущества:
- Не требует настройки
- Работает мгновенно
- Интегрирован в редактор

---

## Способ 2: Локальный анализ через SonarScanner CLI

### Шаг 1: Установить SonarScanner

**Вариант A: через npm (рекомендуется)**
```bash
npm install -g sonarqube-scanner
```

**Вариант B: через Docker**
```bash
docker pull sonarsource/sonar-scanner-cli
```

### Шаг 2: Получить токен SonarCloud

1. Откройте https://sonarcloud.io
2. Войдите в аккаунт
3. Перейдите: **My Account → Security**
4. Создайте токен (Token type: `Project Analysis`)
5. Скопируйте токен

### Шаг 3: Установить токен

**Вариант A: Переменная окружения (Windows)**
```powershell
$env:SONAR_TOKEN="your_token_here"
```

**Вариант B: Файл .env**
```bash
# Создайте файл .env в корне проекта
echo SONAR_TOKEN=your_token_here >> .env
```

### Шаг 4: Запустить анализ

**Вариант A: Через скрипт (рекомендуется)**
```bash
python scripts/run_sonarqube.py
```

**Вариант B: Напрямую через npm**
```bash
npm run sonar
```

**Вариант C: Напрямую через sonar-scanner**
```bash
sonar-scanner
```

**Вариант D: Через Docker**
```bash
docker run --rm \
  -v "$(pwd):/usr/src" \
  -e SONAR_TOKEN="your_token_here" \
  sonarsource/sonar-scanner-cli
```

### Результаты:
После выполнения анализа результаты будут доступны:
- 🔗 https://sonarcloud.io/project/overview?id=GVMBT_PVNDORA_AI_SHOP

---

## Способ 3: Автоматический анализ через GitHub Actions (CI/CD)

### Как работает:
- ✅ Автоматически запускается при создании Pull Request
- ✅ Результаты появляются в SonarCloud Dashboard
- ✅ Quality Gates могут блокировать merge PR

### Настройка:
1. Добавьте секрет `SONAR_TOKEN` в GitHub:
   - Репозиторий → Settings → Secrets and variables → Actions
   - New repository secret: `SONAR_TOKEN` = ваш токен

2. Создайте Pull Request:
   ```bash
   git checkout -b feature/my-feature
   git push origin feature/my-feature
   # Создайте PR через GitHub UI
   ```

3. GitHub Actions автоматически запустит анализ:
   - Job "SonarCloud Analysis" появится в Checks
   - Результаты появятся в SonarCloud Dashboard

### Преимущества:
- ✅ Автоматический анализ при каждом PR
- ✅ История изменений качества кода
- ✅ Интеграция с GitHub

---

## Способ 4: Вручную через SonarCloud Dashboard

1. Откройте https://sonarcloud.io/organizations/gvmbt/projects
2. Нажмите "Analyze a new project"
3. Выберите репозиторий `pvndora`
4. Следуйте инструкциям по настройке

---

## Сравнение способов

| Способ | Скорость | Автоматизация | История | Рекомендация |
|--------|----------|---------------|---------|--------------|
| **SonarLint** | ⚡ Мгновенно | ✅ Автоматически | ❌ Только локально | Для разработки |
| **SonarScanner CLI** | 🐢 2-5 мин | ⚠️ Вручную | ✅ В SonarCloud | Для локального тестирования |
| **GitHub Actions** | 🐢 5-10 мин | ✅ Автоматически | ✅ Полная история | Для CI/CD |
| **SonarCloud UI** | 🐢 2-5 мин | ⚠️ Вручную | ✅ В SonarCloud | Для одноразового анализа |

---

## Рекомендуемый workflow

1. **Во время разработки**: Используйте SonarLint в VS Code (автоматически показывает проблемы)

2. **Перед коммитом**: Запустите локальный анализ
   ```bash
   python scripts/run_sonarqube.py
   ```

3. **При создании PR**: GitHub Actions автоматически запустит анализ

4. **Проверка результатов**: Откройте SonarCloud Dashboard

---

## Troubleshooting

### Ошибка: "sonar-scanner: command not found"
```bash
npm install -g sonarqube-scanner
```

### Ошибка: "SONAR_TOKEN not found"
Убедитесь, что токен установлен:
```bash
# Windows PowerShell
$env:SONAR_TOKEN="your_token"

# Linux/Mac
export SONAR_TOKEN="your_token"
```

### Ошибка: "Project not found"
Проверьте `sonar-project.properties`:
- `sonar.projectKey=GVMBT_PVNDORA_AI_SHOP`
- `sonar.organization=gvmbt`

### Анализ слишком долгий
Исключите ненужные файлы в `sonar-project.properties`:
```properties
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**
```
