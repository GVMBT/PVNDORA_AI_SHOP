# Описание проекта: AI-Маркетплейс цифровых товаров в Telegram

## Концепция и Философия

Мы создаем не просто бот-магазин, а Интеллектуального Консультанта. Мы ломаем привычный паттерн "Категории -> Кнопки -> Корзина". Пользователь часто не знает, какой именно товар ему нужен (какая подписка, какой сервис). Он приходит с проблемой (например, "нужно делать презентации").

Задача ИИ — понять проблему, подобрать лучшее решение из наличия (Склад) и мягко подвести к покупке.

## Ниша и Товары

**Ниша:** Продажа доступов к платным нейросетям и сервисам.

**Типы товаров:**
- Студенческие подписки (Edu)
- Триальные периоды (Trial)
- Шеринг-аккаунты (Shared)
- Ключи API

**Основные продукты:** ChatGPT Plus, Midjourney, Claude Pro, Github Copilot, Canva Pro, Flux, gemini pro итд.

**Важно:** Перечисленные сервисы — это примеры. Система должна поддерживать возможность "файнтюнить" ассистента по актуальным сервисам и подпискам. Админ должен иметь возможность обновлять базу знаний о товарах, их характеристиках, инструкциях и особенностях без изменения кода.

**Формат выдачи:** Текстовые данные (Login:Pass) или инвайт-ссылки.

**Управление сроками подписок:**
- Если аккаунт уже имеет срок действия (например, подписка на год, но уже месяц использована), в `stock_items.expires_at` указывается реальная дата истечения
- При продаже `orders.expires_at` копируется из `stock_items.expires_at` — покупатель получает оставшийся срок
- Если срок считается от момента покупки, используется `products.duration_days` и `orders.expires_at = now() + duration_days`
- Система всегда показывает покупателю реальный срок действия подписки

**Динамическая система скидок на основе простоя аккаунта:**
- Скидка рассчитывается автоматически в зависимости от времени простоя аккаунта в магазине
- Время простоя = разница между `stock_items.created_at` (когда аккаунт добавлен в магазин) и датой продажи
- Чем дольше аккаунт не продан, тем больше скидка (до 20%)
- Пример: аккаунт на 365 дней добавлен в магазин, если продается через месяц — применяется скидка (например, 5-7%), через 2 месяца — больше (10-12%), и т.д.
- Формула расчета скидки:
  - `days_in_stock = (now() - stock_items.created_at)::integer` (дни простоя)
  - `total_duration = products.duration_days` или `(stock_items.expires_at - stock_items.created_at)::integer` (общий срок подписки)
  - `discount_percent = min(20, (days_in_stock / total_duration) * discount_coefficient * 100)`
  - `discount_coefficient` — коэффициент (например, 0.5 или 1.0), может быть настроен админом или быть фиксированным
- Скидка применяется только для аккаунтов с длительным сроком (например, от 30 дней и более)
- Минимальный простой для применения скидки: например, 7 дней (настраивается)
- Скидка сохраняется в `orders.discount_percent` и `orders.original_price` для истории
- AI-консультант упоминает скидку при предложении аккаунтов с простойем: "Скидка X% за простой Y дней"

**Система гарантий:**
- Триальные подписки (trial): гарантия 24 часа (1 день)
- Годовые подписки: гарантия 2 недели (14 дней)
- Гарантия хранится в `products.warranty_days` и применяется при создании тикета на замену
- В течение гарантийного срока пользователь может запросить замену без дополнительных вопросов

## Бизнес-логика и Функционал

### 🧠 AI-Продавец (Domain Expert)

**Мозг:** Google Gemini 2.5 Flash.

**База знаний:**
- Бот разбирается в типах подписок
- Знает, что ChatGPT Plus работает с файлами, а nano banana — только с картинками
- Понимает разницу между "Личный аккаунт" и "Shared (Общий)"
- База знаний настраивается и обновляется через админ-панель (файнтюнинг по актуальным сервисам и подпискам)

**Живое общение:**
- Ассистент должен быть живым, а не роботом
- Если пользователь здоровается — ассистент здоровается в ответ естественным образом
- Поддерживает неформальное общение, шутки, эмодзи
- Адаптирует стиль общения под пользователя

**Управление инвентарем:**
- Интеграция с Supabase
- Никогда не продает то, чего нет
- Если товара нет, предлагает альтернативы или запись в Лист Ожидания (Waitlist)

### 💳 Гибридная оплата (Web App)

Оплата происходит внутри Telegram Mini App, чтобы принимать рубли (СБП) без комиссии платформ.

- Бот генерирует ссылку на оплату конкретного товара
- Web App открывается для проведения транзакции и отображения витрины

### 🤝 VIP Реферальная система

- Обычные пользователи: 20%
- Ключевые партнеры (VIP): Повышенный % (до 50%) — для админов каналов

### 🛠 Поддержка и Сервис

- Распознает запросы на помощь и перенаправляет к администратору или в раздел поддержки
- Проверяет статус пользователя (бан/блокировка) перед ответом

## Живой сценарий (User Flow)

### Этап 1: Выявление потребности (Discovery)

**User:** "Слушай, я видел, как делают такие реалистичные фотки людей, но не могу понять, где это. Миджорни вроде сложно..."

**AI:** (Анализирует: "реалистичные фотки", "не Миджорни". В наличии есть Flux)

**AI (Ответ):** "Скорее всего, вы видели результаты Flux или новой версии Stable Diffusion. Они сейчас топы по реализму. У нас как раз есть доступ к настроенному облачному аккаунту Flux Pro. Это проще, чем Midjourney, и качество фотореализма выше. Стоит 300₽ за месяц. Хотите попробовать?"

### Этап 2: Работа с возражениями (Consultation)

**User:** "А там ВПН нужен? Я в этом не шарю."

**AI:** (Проверяет инструкции товара: "Работает без VPN")

**AI (Ответ):** "Нет, для этого сервиса VPN не нужен, всё открывается прямо в браузере. Вы получаете логин и пароль, заходите и творите. Инструкцию я пришлю сразу после оплаты."

### Этап 3: Закрытие сделки (Closing)

**User:** "Окей, 300р не жалко. Давай."

**AI:** (Детектирует интент на покупку. Выбирает товар)

**AI (Действие):** "Отлично! Оформил заказ на Flux Pro (1 месяц)." (Показывает кнопку: 💳 Оплатить 300₽ — открывает Web App)

### Этап 4: Покупка и Выдача (Fulfillment)

**User:** Оплачивает картой внутри Web App

**System:** Списывает товар, начисляет рефералку

**AI:** "Оплата прошла успешно! ✅ Вот ваш доступ: Логин: user@flux Пароль: 12345 Приятного творчества! Если будут вопросы по генерации — пишите сюда, я подскажу."

## Полный список требований заказчика

### 💳 Платежи и Фулфилмент

**Бизнес-модель:** Работа "под заказ" с товаром "скоропортом" (подписки с ограниченным сроком).

**Стратегия:**
- Если товар есть в наличии → выдача сразу после оплаты (Instant Fulfillment)
- Если товара нет → предоплата → изготовление/получение от поставщика → выдача (Prepaid Order)
- Если товара недостаточно → автоматическое разделение на instant (в наличии) + prepaid (под заказ)
- В случае проблем (таймаут, невозможность получить товар) → автоматический возврат средств на баланс пользователя

**Управление корзиной:**
- Корзина хранится в Redis до оплаты (TTL 24 часа)
- Пользователь может редактировать корзину: изменять количество, удалять товары, отменять запросы
- Заказы в БД создаются только при checkout (нажатие "Оплатить")
- AI работает с корзиной через Function Calling (`get_user_cart`, `add_to_cart`, `update_cart`)

Заказчик хочет, чтобы:

- Пользователь мог оплачивать товары рублями (СБП, карты) через интеграцию с AAIO/ЮKassa/CryptoPay внутри Web App
- **Для товара в наличии:** После успешной оплаты система автоматически выдавала товар (Login:Pass, Cookie, инвайт-ссылку) прямо в чат Telegram
- **Для товара под заказ:** После предоплаты система уведомляла поставщика, отслеживала статус выполнения, и выдавала товар когда он готов
- **При проблемах:** Если товар не может быть получен в срок или по запросу пользователя → автоматический возврат средств на внутренний баланс пользователя с уведомлением
- См. детали в `docs/ON_DEMAND_ORDERS.md`
- При выдаче товара система устанавливает `orders.expires_at`:
  - Если в `stock_items.expires_at` указана дата — копируется она (покупатель получает оставшийся срок)
  - Если даты нет, но есть `products.duration_days` — рассчитывается от момента покупки
  - Покупатель всегда видит реальный срок действия подписки в сообщении о выдаче
- При расчете цены применяется динамическая скидка на основе простоя аккаунта:
  - Рассчитывается время простоя: `days_in_stock = now() - stock_items.created_at`
  - Рассчитывается скидка: `discount_percent = min(20, (days_in_stock / total_duration) * discount_coefficient * 100)`
  - Сохраняется в `orders.original_price` (исходная цена) и `orders.discount_percent` (примененная скидка)
  - Финальная цена = `products.price * (1 - discount_percent / 100)`
- Скидка отображается в Web App и в сообщениях бота с указанием причины (например, "Скидка 10% за простой 2 месяца")

### 🔗 Интеграция Бот ↔ Web App

Заказчик хочет, чтобы:

- При детектировании намерения купить ("Давай", "Оформляю") бот генерировал уникальную ссылку на Web App: `https://t.me/bot/app?startapp=buy_{product_id}_{user_id}`
- Web App получал данные о товаре через REST API (`/api/products/{id}`)
- Web App создавал заказ через API (POST `/api/orders`) и инициировал платеж
- После оплаты backend списывал stock_item (помечал `is_sold=true`) и отправлял товар пользователю

### 🤝 Реферальная система

Заказчик хочет, чтобы:

- Пользователь мог пригласить друга по ссылке `/start ref_{telegram_id}`
- При первом запуске бота система сохраняла `referrer_id` в таблицу `users`
- При покупке реферала бот начислял процент (20% для обычных, до 50% для VIP) на баланс пригласившего
- VIP-статус и процент настраивались индивидуально через поле `personal_ref_percent`

### 📦 Уведомление поставщиков

Заказчик хочет, чтобы:

- После продажи товара система автоматически отправляла Telegram-сообщение поставщику (по `supplier.telegram_id`)
- В сообщении была информация: какой товар продан, кому, за сколько

### 🎟️ Система поддержки (Tickets)

Заказчик хочет, чтобы:

- Если пользователь написал "Не работает", "Хочу замену", бот распознавал проблему и предлагал создать тикет
- Бот проверял гарантийный срок: если заказ в пределах `products.warranty_days` — автоматически одобряет замену
- Бот спрашивал: "Открыть запрос на замену? Опишите проблему."
- Тикет сохранялся в таблицу `tickets` со статусом `open` (или `approved` если в гарантийном сроке)
- Админ видел тикеты в админ-панели и мог одобрить/отклонить замену
- При одобрении пользователь получал новый товар, старый помечался как замененный
- Гарантийные сроки: триалы — 24 часа, годовые — 2 недели

### 🔔 Waitlist и уведомления о возврате товаров

Заказчик хочет, чтобы:

- Когда товар со статусом `coming_soon` или `discontinued` снова становился `active`, система автоматически рассылала уведомления всем пользователям из Waitlist
- Сообщение: "Товар {name}, который вы ждали, снова в наличии! Успейте купить."

### ⭐ Система отзывов и кэшбэк

Заказчик хочет, чтобы:

- Через 1 час после покупки бот отправлял сообщение: "Как товар? Оставьте отзыв и получите 5% кэшбэка на баланс!"
- Если пользователь оставил отзыв (текст/оценку), система начисляла 5% от суммы покупки на `users.balance`
- Отзывы сохранялись для будущей аналитики

### 📅 Lifecycle Management (Управление подписками)

Заказчик хочет, чтобы:

- Система отслеживала истечение подписок (по `orders.expires_at`)
- За 3 дня до истечения бот напоминал: "Ваша подписка ChatGPT заканчивается 25 ноября. Продлить со скидкой 10%?"
- День-в-день после истечения отправлял: "Подписка истекла. Хотите продлить?"

### 🔁 Re-engagement (Возврат молчунов)

Заказчик хочет, чтобы:

- Если пользователь не писал боту 24 часа, система отправляла мягкое напоминание: "Нашли что-то интересное? Новинки недели: Flux Pro, Claude Opus."
- Не спамить: делать такие напоминания не чаще 1 раза в 72 часа

### 📊 Аналитика и события

Заказчик хочет, чтобы:

- Система логировала все события в таблицу `analytics_events`:
  - `view` — пользователь спросил про товар
  - `checkout` — бот предложил купить
  - `pay` — оплата прошла
- Админ мог видеть воронку продаж в Web App: сколько людей спросили → сколько кликнули "Купить" → сколько оплатили

### 🛡️ Безопасность и модерация

Заказчик хочет, чтобы:

- Админ мог банить пользователей через Web App (установка `is_banned=true`)
- Забаненный пользователь получал сообщение: "Доступ ограничен. Обжаловать: @admin."
- Админ мог выдавать предупреждения (`warnings_count++`), после 3 предупреждений — автобан

### 📢 Broadcast (Рассылка)

Заказчик хочет, чтобы:

- Админ мог отправить массовое сообщение всем пользователям через Web App
- Например: "Вышел Midjourney V7! Успей попробовать первым."
- Учитывать флаг `do_not_disturb` — не слать тем, кто отписался

### 🌍 Мультиязычность

Заказчик хочет, чтобы:

- Бот автоматически определял язык пользователя (из `message.from_user.language_code`)
- Все сообщения (включая ошибки, системные уведомления) были на языке пользователя
- Бот поддерживает 9 самых популярных языков: Русский, Английский, Украинский, Немецкий, Французский, Испанский, Турецкий, Арабский, Хинди
- Переводчик и локализация всех текстов (включая AI-промпты) для каждого языка

**Нативная локализация профиля бота:**
- Бот должен иметь переведенное название и описание в интерфейсе Telegram **до того, как пользователь нажмет /start**
- Это реализуется через методы `setMyDescription` и `setMyName` с параметром `language_code`
- При отправке ссылки на бота (t.me/mybot) пользователь должен видеть превью на своем родном языке
- Превью реализуется через `setMyShortDescription` с параметром `language_code`
- Настройка выполняется один раз при деплое/обновлении бота для всех поддерживаемых языков

## Улучшения пользовательского опыта

### 🎮 Геймификация "Money Saved"

Заказчик хочет, чтобы:

- Система рассчитывала экономию пользователя: `MSRP - Price` при каждой покупке
- Экономия накапливалась в поле `users.total_saved`
- Лидерборд отображал топ пользователей по экономии
- Лидерборд реализован на базе Redis Sorted Sets (ZSET) для производительности
- Обновление лидерборда происходит через QStash после каждой покупки
- Пользователь видит свой ранг и общую экономию в профиле Mini App

### 🔗 Виральность (Viral Sharing)

Заказчик хочет, чтобы:

- Пользователь мог поделиться предложением товара через нативный механизм Telegram
- Использование `switchInlineQuery` вместо копирования ссылок
- При клике на shared результат открывается чат с ботом с предзаполненным контекстом
- Это повышает конверсию и упрощает процесс шеринга

### 🎯 Первичная онбордация

Заказчик хочет, чтобы:

**Нативная локализация профиля:**
- Бот должен иметь переведенное название и описание в интерфейсе Telegram **до того, как пользователь нажмет /start**
- Это реализуется через методы `setMyDescription` и `setMyName` с параметром `language_code`
- При отправке ссылки на бота (t.me/mybot) пользователь должен видеть превью на своем родном языке
- Превью реализуется через `setMyShortDescription` с параметром `language_code`
- Настройка выполняется один раз при деплое/обновлении бота для всех поддерживаемых языков

**Приветственное сообщение:**

При первом `/start` бот давал понятное объяснение + примеры запросов:

```
Привет! Я AI-консультант по доступам к нейросетям. Просто напиши, что хочешь делать:
• "Генерировать картинки"
• "Написать курсовую"
• "Помощь с кодом"

Или скажи "Что у вас есть?" — покажу каталог.
```

### 📜 История покупок

Заказчик хочет, чтобы:

- Команда `/my_orders` показывала последние покупки с датами истечения
- AI распознавал запросы типа "Что я покупал?" и выдавал список из БД
- В Web App был раздел "Мои покупки" с функцией "Купить еще раз"

### ⚖️ Сравнение товаров

Заказчик хочет, чтобы:

- На вопрос "Что лучше — ChatGPT или Claude?" AI генерировал сравнительную таблицу с ключевыми характеристиками

### 🎫 Промокоды

Заказчик хочет, чтобы:

- Пользователь мог вводить промокод (например, "FLUX10") для получения скидки
- Таблица `promo_codes` в БД: `code`, `discount_percent`, `expires_at`, `usage_limit`
- Автоматическое применение скидки при оплате в Web App

### 📚 FAQ и база знаний

Заказчик хочет, чтобы:

- Была таблица `faq` с часто задаваемыми вопросами
- AI сначала проверял FAQ перед генерацией ответа (быстрее и точнее)
- В Web App был раздел FAQ для самообслуживания

### ❤️ Wishlist (Список желаний)

Заказчик хочет, чтобы:

- Пользователь мог сохранить товар "на потом" с помощью команды или фразы "Добавь в избранное"
- Команда `/wishlist` показывала сохраненные товары
- Через 3 дня система напоминала о сохраненных товарах с персональным предложением

### ⭐ Социальные доказательства

Заказчик хочет, чтобы:

В карточке товара AI показывал рейтинг, количество покупок и свежие отзывы:

```
Flux Pro — 300₽/мес
⭐️ 4.9/5 (127 отзывов)
🔥 84 покупки за неделю
💬 "Лучше Midjourney!" — @user, вчера
```

### 📢 Персонализированные предложения

Заказчик хочет, чтобы:

- Система отслеживала интересы пользователя (что смотрел, но не купил)
- Через 2 дня: "ChatGPT со скидкой 20% только сегодня!"
- Если покупал давно: "Midjourney V7 вышел. Апгрейд за полцены?"
- Пользователь мог настроить частоту уведомлений

### 🛒 Кросс-продажи

Заказчик хочет, чтобы:

После покупки AI предлагал дополнительные товары:

```
✅ ChatGPT Plus активирован!
70% клиентов также берут:
• Midjourney — 300₽
• Grammarly Premium — 150₽

Оформить со скидкой 10%?
```

### 💰 Возврат средств

Заказчик хочет, чтобы:

- Была возможность запросить возврат в течение 24 часов
- Флаг `orders.refund_requested = true` → админ одобряет → деньги на `users.balance`

### 🌐 Роль Web App

Заказчик хочет, чтобы:

Web App был минималистичной прокладкой к оплате. Основное взаимодействие — в чате с ботом.

**Mini App (Пользовательский интерфейс):**
- Страницу оплаты (минимум элементов)
- Историю покупок
- FAQ
- Витрина товаров

**Админ-панель:**
- Выносится в отдельное приложение или реализуется через Low-Code инструменты (например, Retool, Supabase Studio)
- Не входит в состав Mini App для снижения сложности

## Технический стек

- **Runtime:** Vercel Serverless (Python 3.12)
- **Платформа:** Telegram Bot (Python/aiogram 3.22+) + Telegram Mini App
- **Фреймворк:** FastAPI (ASGI wrapper) + Aiogram 3.x (Webhook mode)
- **Фронтенд:** React, Vite, TailwindCSS
- **База данных:** Supabase (PostgreSQL) + vecs (pgvector client для RAG)
- **AI:** google-genai SDK (Gemini 2.5 Flash)
- **FSM Storage:** RedisStorage (Upstash HTTP Redis)
- **Платежи:** 1Plat
- **Деплой:** Vercel

**Важные технические ограничения:**
- Vercel не поддерживает более 12 serverless функций — архитектура должна быть оптимизирована под это ограничение
- Необходимо использовать монолитную структуру API (единая точка входа `api/index.py`)
- Vercel Fluid Compute: maxDuration = 300s, memory = 1024MB

**Критические правила реализации:**

1. **Гибридная Асинхронная Архитектура:**
   - **Для UX и Диалогов (Best-Effort):** Использовать `fastapi.BackgroundTasks` в связке с Vercel Fluid Compute для быстрого отклика бота
   - **Для Транзакций (Guaranteed Delivery):** Все критически важные процессы (платежи, выдача товара, начисление бонусов, уведомление поставщиков) должны обрабатываться через внешнюю очередь **Upstash QStash**. Это гарантирует выполнение и предоставляет механизмы повтора (Retries)
   - См. детали в `docs/async-architecture.md`

2. **AI-Ядро: Гибридная Стратегия для Детерминизма:**
   - **RAG (Discovery):** Использовать для понимания намерений пользователя
   - **Function Calling (Accuracy):** AI обязан использовать вызовы функций для получения актуальных данных о наличии и динамических скидках в реальном времени из Supabase
   - **Structured Outputs (Determinism):** Все действия AI обязаны использовать Structured Outputs (Pydantic Schema-Constrained Decoding) через SDK google-genai v1.0+. **Финальный формат ответа:** `{"thought": "...", "reply_text": "...", "action": "offer_payment"|"add_to_waitlist"|"show_catalog"|None, "product_id": "..."}`
   - **Context Caching:** Использование Gemini Context Caching API для снижения стоимости и задержки обработки System Prompt
   - **Vibe Coding:** Генерация эмоционально окрашенных, контекстных ответов для повышения вовлеченности
   - **Human Handoff:** Структурированный процесс поддержки с эскалацией на оператора
   - **Надежность:** Использовать библиотеку `tenacity` для реализации паттерна Retry при ошибках валидации Pydantic
   - См. детали в `docs/ai-strategy.md`

3. **Гибридная Модель Управления Инвентарем:**
   - **System Prompt (Скорость):** Базовое описание ассортимента для быстрого диалога
   - **Real-time Validation (Надежность):** Обязательная валидация наличия и цены через прямой запрос к БД (Supabase RPC) или AI Function Calling в момент генерации действия `offer_payment`
   - Это предотвращает риск перепродажи (Overselling)
   - См. детали в `docs/async-architecture.md`

4. **Транзакционная Целостность:**
   - **Проактивное Резервирование:** Товар должен быть зарезервирован до начала оплаты
   - **Управление корзиной:** Корзина хранится в Redis до оплаты, заказы в БД создаются только при checkout
   - **Автоматическое разделение:** При недостаточном наличии система автоматически разделяет на instant + prepaid
   - **Атомарные Транзакции:** Вся транзакционная логика (проверка наличия -> расчет скидки -> резервирование -> создание заказа) переносится внутрь **Хранимых Процедур PostgreSQL** и вызывается через `supabase.rpc()` в рамках единой транзакции
   - Отказ от реактивной обработки Race Conditions (возврат денег после ошибки)

5. **Supabase Integration:**
   - НЕ использовать realtime-py listeners (websockets не работают в serverless)
   - Использовать vecs для RAG (product search): создать collection `products` с dimension 768 (Gemini embeddings)
   - См. детали в `docs/database-schema.md`

6. **Aiogram 3.x:**
   - Использовать Dependency Injection через Middleware для передачи supabase и genai_client в handlers
   - Использовать RedisStorage (через Upstash HTTP Redis) для FSM. НЕ использовать MemoryStorage

7. **Мультиязычность:**
   - **Configuration-time (Нативная):** Локализация профиля бота (Имя, Описание) через скрипты настройки
   - **Runtime (I18n):** Реализация I18n Middleware в Aiogram для локализации всех сообщений интерфейса бота и системных промптов AI в реальном времени
   - **RTL Поддержка:** Обязательная поддержка Right-to-Left интерфейсов для арабского языка
   - **Культурная Адаптация:** Адаптация тона AI под культурные особенности каждого языка
   - См. детали в `docs/ux-deep-linking.md`

8. **Web App Integration:**
   - **Mini App:** Фокусируется только на пользовательском опыте (оплата, история, витрина, лидерборд)
   - **Админ-панель:** Выносится в отдельное приложение или реализуется через Low-Code инструменты (например, Retool, Supabase Studio)
   - **Deep Linking:** Бесшовная передача контекста через startapp с использованием Base64url кодирования
   - **Виральность:** Нативный шеринг предложений через `switchInlineQuery` вместо копирования ссылок
   - См. детали в `docs/ux-deep-linking.md`

9. **Платежный Поток (Middleman Pattern):**
   - Использование только внешних платежных шлюзов (1Plat)
   - Нативные платежи Telegram (Stars) не используются
   - См. детали в `docs/async-architecture.md`

**Оптимизация производительности:**

- Динамические скидки: создавать SQL View `available_stock_with_discounts` в Supabase, чтобы SQL считал скидку на лету
- Бот запрашивает уже готовую цену: `SELECT * FROM available_stock_with_discounts` (быстрее и дешевле для Serverless)

**DevOps и Инфраструктура:**

- **Локальная разработка:** Обязательное использование Docker и supabase-cli для локального запуска полного стека Supabase (включая pgvector), обеспечивая консистентность сред
- **CI/CD:** Обязательное внедрение внешнего CI/CD пайплайна (например, GitHub Actions) для автоматического тестирования, проверки качества кода (CI) и оркестрации развертывания (CD), включая миграции БД и запуск конфигурационных скриптов Telegram после деплоя на Vercel
- См. детали в `docs/cicd-pipeline.md`

## Переменные окружения

Следующие переменные окружения должны быть настроены в проекте:

**Telegram:**
- `TELEGRAM_WEBHOOK_URL` — URL для вебхука Telegram бота
- `TELEGRAM_TOKEN` — токен Telegram бота

**Supabase:**
- `SUPABASE_URL` — URL проекта Supabase
- `SUPABASE_SERVICE_ROLE_KEY` — сервисный ключ Supabase для административных операций

**AI:**
- `GEMINI_API_KEY` — API ключ для Google Gemini 2.5 Flash

**Upstash:**
- `UPSTASH_REDIS_URL` — Upstash Redis URL для FSM storage, корзин и кэширования
- `UPSTASH_REDIS_TOKEN` — Upstash Redis token
- `QSTASH_TOKEN` — Upstash QStash token для очередей
- `QSTASH_URL` — Upstash QStash URL (обычно https://qstash.upstash.io/v2)

**Настройка:** Upstash имеет официальную интеграцию с Vercel. Можно настроить через Vercel Dashboard → Settings → Integrations → Upstash (автоматическая синхронизация переменных), либо вручную через [console.upstash.com](https://console.upstash.com/). См. детали в `docs/QSTASH_EXPLAINED.md`.

**Платежи:**
- `ONEPLAT_SHOP_ID` — ID магазина 1Plat (x-shop)
- `ONEPLAT_SECRET_KEY` — Секретный ключ 1Plat (x-secret)
- `YUKASSA_SECRET_KEY` — Секретный ключ ЮКассы

Уже настроены в vercel

