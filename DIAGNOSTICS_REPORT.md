# 🔍 Диагностический отчет: Проблемы с заказами и оплатой

**Дата:** 2026-01-11  
**Статус:** Требуется исправление

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. HTTP 500 при оплате с баланса

**Симптом:** Пользователь видит "HTTP 500" при попытке оплатить заказ с баланса.

**Endpoint:** `POST /api/webapp/orders` (с `payment_method: "balance"`)

**Потенциальные причины:**

1. **Проблема в `_process_balance_payment()` (core/routers/webapp/orders/payments.py:419-528)**
   - Строка 457: `db.client.rpc("add_to_user_balance", ...)` может падать
   - Строка 473-482: `OrderStatusService.mark_payment_confirmed()` может падать
   - Строка 485-502: QStash publish может падать

2. **Отсутствие обработки ошибок в критических местах**

**Рекомендации:**
- Добавить детальное логирование в `_process_balance_payment()`
- Проверить что RPC функция `add_to_user_balance` существует в БД
- Проверить что `OrderStatusService` инициализируется корректно

**Код для проверки:**
```python
# core/routers/webapp/orders/payments.py:456-469
try:
    await db.client.rpc("add_to_user_balance", {
        "p_user_id": db_user.id,
        "p_amount": -to_float(order_total_in_balance_currency),
        "p_reason": f"Payment for order {order.id}"
    }).execute()
except Exception as e:
    # Текущий код удаляет заказ и возвращает 500
    # Нужно добавить детальное логирование
```

---

### 2. Нет заказов в истории (пустой список)

**Симптом:** Пользователь видит "ЗАКАЗОВ НЕТ" на странице заказов.

**Endpoint:** `GET /api/webapp/orders`

**Текущее состояние:**
- DEBUG логи уже добавлены пользователем в `get_webapp_orders()` (crud.py:171-294)
- Код обрабатывает ошибки и продолжает работу (строки 285-290)

**Потенциальные причины:**

1. **Запрос не возвращает данные:**
   - Строка 188: `query = db.client.table("orders").select("*, order_items(*, product:products(*))").eq("user_id", db_user.id)`
   - Возможно `user_id` не совпадает
   - Возможно заказы действительно нет

2. **Ошибка при обработке заказов:**
   - Строка 223-290: Цикл обработки может пропускать все заказы из-за ошибок
   - Ошибки логируются, но заказы не возвращаются

3. **Проблема с JOIN order_items:**
   - Запрос использует nested select: `order_items(*, product:products(*))`
   - Если order_items пустые, заказы могут быть пропущены (строка 226-228)

**Рекомендации:**
- Проверить логи Vercel для DEBUG сообщений
- Проверить что заказы действительно создаются в БД
- Убедиться что `user_id` в заказах соответствует `db_user.id`

---

### 3. Проверка регистрации endpoints

**Текущая структура:**
```
api/index.py
  └─> app.include_router(webapp_router, prefix="/api/webapp")

core/routers/webapp/__init__.py
  └─> router.include_router(orders_router)

core/routers/webapp/orders/router.py
  ├─> router.include_router(crud_router)
  └─> router.include_router(payments_router)

core/routers/webapp/orders/crud.py
  └─> @crud_router.get("/orders")

core/routers/webapp/orders/payments.py
  └─> @payments_router.post("/orders")
```

**Итоговые endpoints:**
- ✅ `GET /api/webapp/orders` → `get_webapp_orders()`
- ✅ `POST /api/webapp/orders` → `create_webapp_order()`
- ✅ `GET /api/webapp/orders/{order_id}/status` → `get_webapp_order_status()`
- ✅ `POST /api/webapp/orders/{order_id}/verify-payment` → `verify_order_payment()`
- ✅ `POST /api/webapp/orders/confirm-payment` → `confirm_manual_payment()`

**Вывод:** Endpoints зарегистрированы правильно.

---

## 🟡 ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ

### 4. Логика обработки заказов без order_items

**Проблема:** В `get_webapp_orders()` (crud.py:226-228):
```python
if not items_data:
    logger.warning(f"Order {row.get('id')} has no order_items")
    continue  # Заказ пропускается!
```

**Влияние:** Заказы без order_items не показываются пользователю.

**Рекомендация:** Решить, нужно ли показывать заказы без items или это ошибка данных.

---

### 5. Ошибки обработки заказов молча игнорируются

**Проблема:** В `get_webapp_orders()` (crud.py:285-290):
```python
except Exception as e:
    error_count += 1
    logger.error(f"[DEBUG] order processing exception: ...")
    continue  # Заказ пропускается, но пользователь не видит ошибку
```

**Влияние:** Если все заказы падают с ошибкой, пользователь видит пустой список.

**Рекомендация:** Возвращать частичный результат или ошибку, если все заказы не удалось обработать.

---

### 6. RPC функция `add_to_user_balance` используется, но определение не найдено

**Проблема:** Функция `add_to_user_balance` используется в 6 местах:
- `core/routers/webapp/orders/payments.py:457`
- `core/routers/admin/users.py:261`
- `core/routers/admin/withdrawals.py:278`
- `core/routers/webapp/misc.py:209`
- `core/routers/workers/payments.py:86`
- `core/agent/tools/checkout.py:292`

**Риск:** Если RPC функция не существует в БД, все эти места будут падать с 500 ошибкой.

**Рекомендация:** 
- Проверить что функция `add_to_user_balance` существует в БД
- Если нет - создать миграцию
- Если есть - проверить сигнатуру (параметры `p_user_id`, `p_amount`, `p_reason`)

---

## 📋 ПЛАН ДЕЙСТВИЙ

### Немедленно (критично)

1. **Получить логи Vercel:**
   ```bash
   python scripts/get_latest_deployment_logs.py --project pvndora
   ```
   Или через Vercel Dashboard: Logs → Runtime Logs

2. **Проверить DEBUG логи в `get_webapp_orders()`:**
   - Проверить что `db_user` найден
   - Проверить количество заказов в `result.data`
   - Проверить ошибки обработки (`error_count`)

3. **Проверить ошибки в `_process_balance_payment()`:**
   - Добавить try/except вокруг каждого критического блока
   - Логировать детальные ошибки
   - Проверить что RPC функция существует

### В течение дня

4. **Добавить детальное логирование в `_process_balance_payment()`:**
   ```python
   logger.info(f"[BALANCE_PAYMENT] Starting for order {order.id}")
   logger.info(f"[BALANCE_PAYMENT] User balance: {user_balance} {balance_currency}")
   logger.info(f"[BALANCE_PAYMENT] Order total: {order_total_in_balance_currency} {balance_currency}")
   ```

5. **Проверить БД:**
   - Существует ли RPC функция `add_to_user_balance`?
   - Есть ли заказы в таблице `orders`?
   - Правильно ли связаны `user_id` в заказах?

6. **Протестировать endpoints вручную:**
   - `GET /api/webapp/orders` с правильным auth
   - `POST /api/webapp/orders` с `payment_method: "balance"`

---

## 🔍 КОМАНДЫ ДЛЯ ДИАГНОСТИКИ

### 1. Получить логи Vercel
```bash
cd d:\pvndora
python scripts/get_latest_deployment_logs.py --project pvndora
```

### 2. Проверить синтаксис Python
```bash
python -m pyflakes core/routers/webapp/orders/payments.py
python -m pyflakes core/routers/webapp/orders/crud.py
```

### 3. Проверить структуру endpoints
```bash
# Все endpoints должны быть доступны через:
# GET /api/webapp/orders
# POST /api/webapp/orders
# GET /api/webapp/orders/{order_id}/status
# POST /api/webapp/orders/{order_id}/verify-payment
# POST /api/webapp/orders/confirm-payment
```

---

## 📊 ПРОВЕРКА ENDPOINTS

| Endpoint | Метод | Статус | Комментарий |
|----------|-------|--------|-------------|
| `/api/webapp/orders` | GET | ✅ Зарегистрирован | `get_webapp_orders()` |
| `/api/webapp/orders` | POST | ✅ Зарегистрирован | `create_webapp_order()` |
| `/api/webapp/orders/{order_id}/status` | GET | ✅ Зарегистрирован | `get_webapp_order_status()` |
| `/api/webapp/orders/{order_id}/verify-payment` | POST | ✅ Зарегистрирован | `verify_order_payment()` |
| `/api/webapp/orders/confirm-payment` | POST | ✅ Зарегистрирован | `confirm_manual_payment()` |

**Вывод:** Все endpoints зарегистрированы правильно. Проблема не в регистрации.

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ **Получить логи Vercel** - понять что именно падает
2. ✅ **Проверить DEBUG логи** - увидеть что происходит с запросом заказов
3. ⚠️ **Исправить ошибки** - на основе логов
4. ⚠️ **Добавить обработку ошибок** - улучшить UX при ошибках
5. ⚠️ **Протестировать** - убедиться что все работает

---

## 📝 ПРИМЕЧАНИЯ

- DEBUG логи уже добавлены пользователем в `get_webapp_orders()` - это хорошо!
- Код обработки ошибок в цикле заказов корректный (продолжает обработку)
- Endpoints зарегистрированы правильно - проблема не в роутинге
- Нужны логи Vercel для точной диагностики
